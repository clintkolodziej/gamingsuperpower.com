@page
@model MarkdownPageModel

@{
    ViewBag.Title = "Upscalers";
    ViewBag.Description = "Upscalers";
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/upscalers.css")">
}

@section Scripts {

}

@if (Model.SectionExists("intro"))
{
<section id="intro">

	<div class="container">

        @Model.GetSection("intro")

	</div>

</section>
}

@if (Model.SectionExists("summary"))
{
<section id="summary">

	<div class="container">

        @Model.GetSection("summary")

	</div>

</section>
}