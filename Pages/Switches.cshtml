@page
@model MarkdownPageModel

@{
    ViewBag.Title = "Switches";
    ViewBag.Description = "Switches";
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/switches.css")">
}

@section Scripts {

}

@if (Model.SectionExists("intro"))
{
<section id="intro">

    @Model.GetSection("intro")

</section>
}

@if (Model.SectionExists("summary"))
{
<section id="summary">

    @Model.GetSection("summary")

</section>
}